
<form method="post" action="{{ path('admin_property_delete', {id: property.id}) }}" style="display: inline-block"
      onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce bien ?')">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property.id) }}">
    <button class="btn-outline border-0" title="Supprimer"><i class="far fa-trash-alt "></i></button>
</form>