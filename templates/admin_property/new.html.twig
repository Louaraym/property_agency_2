{% extends 'base.html.twig' %}

{% block title %}New Property{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un bien </h1>
        <div class="text-right mb-4">
            <a href="{{ path('admin_property_index') }}" class="btn btn-primary mt-2">Retour à la liste</a>
        </div>
        {{ include('admin_property/_form.html.twig', {'button_label': 'Update'}) }}

    </div>
{% endblock %}
